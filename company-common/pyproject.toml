[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "company-common"
version = "0.1.0"
description = "Shared company data models"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
]

[tool.setuptools]
packages = ["models"]